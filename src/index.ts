export { LICENSE_TOUCH } from './constants';
export {
  LicenseTouchClient,
  isLicenseQuotaExceeded,
  isLicenseTouchUnauthorized,
  touchLicenseSeat,
} from './touch-client';
export type {
  LicenseTouchClientOptions,
  LicenseTouchResult,
  TouchLicenseSeatInput,
} from './types';
export { LicenseTouchError } from './types';
